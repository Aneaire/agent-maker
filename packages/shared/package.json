{
  "name": "@agent-maker/shared",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx convex dev --env-file ../../.env",
    "deploy": "bunx --env-file=../../.env convex deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "convex": "^1.21.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.8.0"
  }
}
